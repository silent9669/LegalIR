You are the senior ML/IR architect for silent9669/LegalIR. Treat the markdown package in `new_spec_legalir' as authoritative. Build a fresh Task1 pipeline that inherits the canonical v2 dataset unchanged. Replace monolithic Kaggle FULL with a resumable Validation/Artifact Factory plus a minimal Kaggle Final Trainer. Compute Legal/PyVi/DEk21/Exact static retrieval once for all 7k train+1k public queries, cache it without qrels, keep Question Memory fold-local, and use one Arrow-backed lazy MacroEvidenceStore with bounded LRU. Run 5-fold OOF and doc-disjoint as resumable jobs, then freeze production_lock and a verified production bundle containing final pairs/public candidates/evidence/fusion. Kaggle must only verify bundle+dataset, train one final all-7k BGE LoRA, rerank public queries, validate top5, and package submission. Preserve Recall@5 scoring semantics, effective batch 16, zero leakage, <4B, RAM guards, parity tests, CI green, Colab T4 PASS, and runtime pin